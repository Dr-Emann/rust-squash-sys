[package]
name = "squash-sys"
version = "0.1.0"
authors = ["Zachary Dremann <dremann@gmail.com>"]

links = "libsquash0.7"
build = "build.rs"

[dependencies]
libc = "*"
bitflags = "0.3.2"

[build-dependencies]
pkg-config = "0.3.5"
